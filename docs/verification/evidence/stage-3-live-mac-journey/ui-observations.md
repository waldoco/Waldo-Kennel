# Computer Use observations

Captured with Computer Use on the installed `/Applications/Kennel.app` package. The tool returned inline screenshots and accessibility trees; this environment did not expose a local file-export path for those screenshots.

1. Initial paint showed **Kennel daemon is unavailable** and stated that nothing was current until it was running.
2. A subsequent refresh showed the daemon-backed Project board with the pre-existing `Scratch` project under `~/.kennel/data/scratch/default`.
3. Settings showed exactly four sections: `General`, `AI providers & API keys`, `Updates`, and `Help`.
4. `AI providers & API keys` showed Provider, Model, Reasoning effort, API key, Save reasoning settings, Verify now, and Advanced repository inspection limits. It showed no adapter install, harness connection, pairing, challenge, bearer, rotation, or revoke control.
5. Clicking the visible Search button did not open a visible search surface before the run was stopped.
