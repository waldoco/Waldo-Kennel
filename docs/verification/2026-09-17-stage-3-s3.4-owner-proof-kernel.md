# Stage 3 S3.4 owner-proof kernel evidence

- Baseline: S3.2 `6a34ef98b67fbe8a951e9aabd94f3e7365c56f9a`
- Status: kernel locally gate-passed; full S3.4 integration blocked on frozen S3.3 transport surface

Implemented:

- caller-only random owner-proof bearer with only a SHA-256 verifier persisted;
- exact binding to app run, mission, content digest, target, target/question generation, closed command class, expiry, and material confirmation reference;
- routine classes `turn|steer|answer|interrupt|cancel` without a material confirmation reference;
- material classes `replace|approval|accept` requiring an exact confirmation reference;
- atomic one-time verification/consumption, exact mint replay without bearer re-return, and constant-time tuple checks;
- typed SQLite record, store, migration 0146, sqlc output, migration ledger and rollback proof;
- one closed mapping to the S3.1 transport class vocabulary. Mapping does not authenticate either side or grant authority.

Evidence:

- Focused domain, existing owner-command, owner-proof, S3.1 connection, SQLite store and SQLite package tests pass.
- Focused owner-proof race tests pass.
- Migration upgrade/rollback and migration-ledger checks pass.
- `go vet` and `git diff --check` pass.
- Tests cover restart, replay, every tuple field, expiry, material-confirmation absence, concurrent consumption and no verifier in public string/JSON shapes.

Dependency finding:

The proof kernel is not wired to an adapter ingress. S3.4 requires adapter transport authentication and owner proof to remain separate, and the S3.3 pairing/thin-transport surface is not implemented or frozen yet. Guessing a route now would create a second transport contract or weaken the required separation. Call-site extension and candidate promotion therefore wait for accepted S3.3 surfaces. No macOS native proof is claimed.
